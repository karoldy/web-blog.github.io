# xxx科技-前端开发工程师

### 以下代码输出什么

```js
var a = {};
var b = Object.protopyte;

console.log(a.protopyte === b);
console.log(Object.getProtoOf(a) === b);
```

### cookie, sessionStorage, localStorage的区别

### 使用你熟悉的方式对数字数组按从小到大进行排序(不可使用sort)

### 使用递归的方式进行阶乘的计算
